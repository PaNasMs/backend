#!/bin/sh
set -eu
systemctl daemon-reload
echo 'Cooling installed separately from the panel. Run panasms-cooling-configure for this GPIO27 hardware.'
if [ -f /etc/panasms-cooling/config.json ] && systemctl is-enabled --quiet panasms-cooling.service; then
 systemctl restart panasms-cooling.service
fi
